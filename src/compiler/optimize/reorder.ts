import * as ir from '../../ir';

function canReorder(from: ir.IrStmt, to: ir.IrStmt) {
    // TODO
    return false;
}

/**
 * Returns `true` if we can move statement `from` to the current position of
 * statement `to`.
 */
export function tryReorder(from: ir.IrStmt, to: ir.IrStmt): boolean {
    // TODO
    return false;
}
